-- ============================================================
-- Migration 002: Sector schemas (Folder model)
-- Add one CREATE SCHEMA per programmatic sector.
-- ============================================================

CREATE SCHEMA IF NOT EXISTS wash_sector;
CREATE SCHEMA IF NOT EXISTS health_sector;
